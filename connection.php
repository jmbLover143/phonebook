<?php

$server = "localhost";
$user = "root";
$password = "";
$database = "phonebook";

$conn = mysqli_connect($server, $user, $password, $database);

if(!$conn){
    echo "invalid connection.";
} else{
    
}
        
 ?>